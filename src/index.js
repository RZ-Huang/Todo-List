import React from 'react';
import ReactDOM from 'react-dom';
import App from './app.js';

// 當執行的時候，會把 App 的 Component，render到 root 這個 DOM 上面去
ReactDOM.render(<App />, document.getElementById('root'));
